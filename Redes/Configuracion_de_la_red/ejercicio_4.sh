#Si lo queremos hacer de forma temporal tendriamos que borrar la entrada default
#Si lo queremos hacer de forma persistente modificar servidor dhcp
#configuracion estatica seria borrar la entrada de default.Dentro de interfaces